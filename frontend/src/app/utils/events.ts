import { Time } from "@angular/common";

export class Events{
    constructor(
        id: string,
        title: string,
        costumer: string,
        employee: string,
        treatment: string,
        date: Date,
        stertTime: Time,
        endTime: Time
    ){}
}